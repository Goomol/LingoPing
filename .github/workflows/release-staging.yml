name: Release Staging Build

on:
  push:
    branches: [staging]
  workflow_dispatch:

jobs:
  build-staging:
    name: Build & Package Staging Extension
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v7
        with:
          node-version: 22
          cache: 'npm'

      - name: Install Dependencies
        run: npm ci

      - name: Build Monorepo Packages
        run: npm run build:staging

      - name: Run Test Suite
        run: npm run test:all

      - name: Package Staging Extension
        env:
          VITE_SUPABASE_URL: ${{ secrets.STAGING_SUPABASE_URL || 'https://cqyepytfqtatnxdtdsop.supabase.co' }}
          VITE_SUPABASE_PUBLISHABLE_KEY: ${{ secrets.STAGING_SUPABASE_PUBLISHABLE_KEY || 'sb_publishable_jcgU7zl_8sU35qoDkgozQA_rMbm7bfv' }}
        run: npm run pack:staging

      - name: Upload Staging ZIP Artifact
        uses: actions/upload-artifact@v4
        with:
          name: lingoping-staging-build
          path: releases/*.zip
          retention-days: 14
